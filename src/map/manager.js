
import { Map } from '.';

export default class MapManager {
    load(path) {
        let map = new Map();
        return map;
    }
    create(seed) {
        let map = new Map();
        return map;
    }
    save(map) {

    }
}